global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'pin-mate-buy-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets:
          - 'host.docker.internal:8091'
        labels:
          app: 'pin-mate-buy-app'
